# Курсовой проект: Система антифрода для банковских транзакций

## 1. Цель проекта

Разработать прототип системы обнаружения мошеннических транзакций, показав умение:

1. Моделировать предметную область банковских транзакций
2. Реализовать правила обнаружения подозрительной активности
3. Организовать хранение и обработку данных
4. Оценить качество системы

## 2. Что нужно сделать

Создать систему, которая анализирует банковские транзакции и выносит решение:
- **ALLOW** — пропустить транзакцию
- **BLOCK** — заблокировать как подозрительную

## 3. Что сдать

1. **Код** (любой язык программирования):
   - Генератор тестовых транзакций (минимум 10 000 записей)
   - Набор правил обнаружения мошенничества (минимум 4 правила)
   - Модуль проверки транзакций
   - Инструкция по запуску (README)

2. **PDF-отчёт** (10-15 страниц):
   - Описание схемы транзакции и модели данных
   - Описание правил и логики их работы
   - Результаты тестирования (метрики)
   - Примеры работы системы

3. **Готовность к защите**:
   - Демонстрация работы на примерах
   - Объяснение выбранных правил

## 4. Минимальная схема транзакции

```json
{
  "id": "txn_12345",
  "account_id": 123456,
  "amount": 1500.00,
  "timestamp": "2025-11-17T14:30:00Z",
  "merchant_category": "online_shop",
  "location": "Moscow",
  "is_card_present": false
}
```

## 5. Обязательные правила (минимум 4)

Реализуйте хотя бы 4 правила из этого списка:

1. **Большая сумма**: `amount > 50000` → BLOCK
2. **Ночная активность**: транзакция между 2:00 и 5:00 утра → подозрительно
3. **Частые транзакции**: более 5 транзакций за 10 минут → BLOCK
4. **Необычная категория**: редкая для клиента категория мерчанта → проверка
5. **Отсутствие карты**: онлайн покупка на большую сумму без 3D-Secure → BLOCK
6. **Изменение локации**: расстояние > 500 км за короткое время → BLOCK

Каждое правило должно давать баллы. Сумма баллов определяет решение:
- 0-2 балла → ALLOW
- 3+ баллов → BLOCK

## 6. Структура проекта

```
project/
  data/
    generate.py          # генерация тестовых транзакций
    transactions.csv     # сгенерированные данные
  rules/
    engine.py            # движок проверки правил
    rules.py             # определения правил
  tests/
    test_rules.py        # тесты
  main.py                # точка входа
  README.md              # инструкция
```

## 7. Пример правила в коде

```python
def rule_high_amount(transaction):
    """Проверка на большую сумму"""
    if transaction['amount'] > 50000:
        return {
            'triggered': True,
            'score': 3,
            'reason': 'Сумма превышает 50000'
        }
    return {'triggered': False, 'score': 0}
```

## 8. Метрики для оценки

Протестируйте систему на сгенерированных данных и посчитайте:

1. **True Positive (TP)**: правильно заблокированные мошеннические транзакции
2. **False Positive (FP)**: ошибочно заблокированные нормальные транзакции
3. **Precision**: TP / (TP + FP) — точность блокировок
4. **Recall**: TP / (все мошеннические) — полнота обнаружения

Целевые значения:
- Precision > 70%
- Recall > 60%

## 9. Генерация данных

Сгенерируйте минимум 10 000 транзакций:
- 97% нормальные
- 3% мошеннические (с явными паттернами из ваших правил)

Пример:
```python
normal_tx = {
    'amount': random.uniform(100, 5000),
    'hour': random.randint(9, 21),
    'merchant_category': random.choice(['grocery', 'restaurant'])
}

fraud_tx = {
    'amount': random.uniform(60000, 100000),  # большая сумма
    'hour': random.randint(2, 4),             # ночь
    'merchant_category': 'casino'             # подозрительно
}
```

## 10. Структура PDF-отчёта

1. **Титульный лист** (ФИО, группа, тема)
2. **Введение** (1 страница):
   - Зачем нужен антифрод
   - Задача проекта
3. **Модель данных** (2 страницы):
   - Схема транзакции
   - Какие поля используются
4. **Правила** (3-4 страницы):
   - Описание каждого правила
   - Обоснование выбора
   - Примеры срабатывания
5. **Результаты** (2-3 страницы):
   - Метрики (таблица)
   - Примеры правильных/неправильных решений
   - Анализ ошибок
6. **Архитектура** (1-2 страницы):
   - Диаграмма компонентов
   - Как работает система
7. **Выводы** (1 страница):
   - Что получилось
   - Что можно улучшить

## 11. Как запустить

В README.md проекта напишите:

```bash
# Установка зависимостей
pip install -r requirements.txt

# Генерация данных
python data/generate.py --count 10000

# Запуск проверки
python main.py --input data/transactions.csv --output results.csv

# Запуск тестов
pytest tests/
```

## 12. Вопросы к защите

Готовьтесь ответить:

1. Почему выбрали именно эти правила?
2. Как определили пороги (например, 50000 для суммы)?
3. Что такое False Positive и почему это важно?
4. Как улучшить систему для реального банка?
5. Какие данные еще могли бы помочь?

## 13. Оценивание

| Критерий | Баллы |
|----------|-------|
| Генерация данных работает | 15 |
| Реализовано 4+ правил | 25 |
| Правила корректны и обоснованы | 20 |
| Метрики посчитаны и описаны | 15 |
| PDF-отчёт оформлен | 15 |
| Защита (демо + ответы) | 10 |

**Итого: 100 баллов**

Бонусы (+10 каждый):
- Интеграция с Kafka/ClickHouse
- Web-интерфейс для проверки транзакции
- Визуализация результатов (графики)

## 14. Частые ошибки

❌ Не делайте:
1. Слишком простые правила (только 1-2)
2. Нет тестирования на данных
3. Нет обоснования выбора правил
4. PDF без примеров работы
5. Код не запускается

✅ Делайте:
1. Комментируйте код
2. Приводите примеры в отчёте
3. Тестируйте на разных данных
4. Объясняйте логику решений

## 15. Пример демонстрации на защите

Покажите 3-5 транзакций:

```python
# Нормальная транзакция
tx1 = {"amount": 1200, "hour": 15, "category": "grocery"}
# Результат: ALLOW (0 баллов)

# Подозрительная транзакция
tx2 = {"amount": 75000, "hour": 3, "category": "casino"}
# Результат: BLOCK (6 баллов: 3 за сумму + 2 за время + 1 за категорию)
```

## 16. Дедлайн и требования

- Минимум за 3 дня до защиты: код + PDF в репозитории
- Код должен запускаться (проверьте!)
- PDF должен открываться и читаться
- Будьте готовы показать работу вживую

## 17. Где искать помощь

- Документация по Python/вашему ЯП
- Примеры генерации случайных данных
- Калькулятор метрик Precision/Recall
- Примеры других антифрод систем (для вдохновения)

---

**Главное**: сделать работающий прототип, который можно объяснить и продемонстрировать. Не пытайтесь сделать production-ready систему — это курсовая, а не диплом!

Удачи! 🚀
